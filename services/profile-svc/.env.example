# Example .env for profile-svc — DO NOT COMMIT secrets. Copy to .env and fill values.
DB_HOST=your-db-host.example
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=changeme
DB_NAME=railway
DB_SCHEMA=profiles

# Optional
DB_ECHO=False
DB_POOL_SIZE=10
